{
  "name": "football-in-your-hand",
  "version": "0.1.0",
  "private": true,
  "description": "An arcade mobile football game — a faithful, original clone of the Mini Football design, built in TypeScript + HTML5 Canvas with Vite.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
